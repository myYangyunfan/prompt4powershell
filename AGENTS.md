# AGENTS.md

## 项目用途
本仓库是一个**单文件模板项目**，用于生成「PowerShell 环境提示词」。用户填好设备信息后，把整份模板发给 AI agent，由 AI 输出一份适配该设备、可直接复制粘贴使用的最终提示词（约束 AI 只用 PowerShell 语法、不用 Bash）。

## 文件
- `prompt-template.md` —— 唯一的项目文件，即模板本身。无代码、无构建、无测试、无 lint。

## 模板结构（编辑前必读）
`prompt-template.md` 分三部分：
1. **① 设备信息** —— 真人填写的表格（操作系统 / Shell 类型 / Shell 版本 / Shell 绝对路径），含 `【】` 占位符。
2. **② 给 AI agent 的指令** —— 生成规则：固定骨架 + 按 Shell 类型二选一的第 1 条 + 逐字照抄的第 2~5 条。
3. **③ 输出示例** —— Windows 11 / PowerShell 7 / 7.6.5 的成品示范。

## 编辑约定与注意事项
- **第 2~5 条规则是逐字照抄的固定原文**，改动措辞会破坏模板语义；除非用户明确要求，不要重写这些条文。
- 第 1 条规则按 Shell 类型**二选一**：PowerShell 7 用「支持 &&、||、三元运算符」，Windows PowerShell 5.1 用「不支持 &&、||、三元运算符」；两条不能同时出现。
- 所有 `{占位符}`（操作系统、Shell 类型、Shell 版本、Shell 绝对路径）都要替换成设备信息里的值，包括第 5 条内嵌的 `{Shell 类型}` 和 `{Shell 绝对路径}`。
- 模板强调的硬性约束贯穿全文：**默认禁止 Bash 语法和 heredoc**；多行 Python 必须用 PowerShell here-string（`@"..."@`）管道传给 `python -`，严禁 `<<EOF`。
- 示例部分（③）应与前面规则保持一致，修改规则时同步更新示例。
